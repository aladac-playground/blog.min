- if @posts.count > 0
  %table.table
    %tr
      %th Number
      %th Title
      %th TimeStamp
      %th Options
    - @posts.each do |post|
      %tr
        %td= post[:id]
        %td= post[:title]
        %td= post[:created_at]
        %td
          %span.btn Edit
          %a.btn.btn-danger.confirm{:href => "/admin/post_delete/#{post[:id]}"} Delete
  = haml :pagination
- else
  No more posts
